# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local runtime state (per-deployment) — refresh tokens, schedule, events
user_data/

# Secrets (e.g. YOTO_CLIENT_ID picked up by docker-compose)
.env
.env.*
!.env.example
